import type { Metadata } from "next";
import MobileProjectsStoriesNum from "@/components/sections/MobileProjectsStoriesNum";

export const metadata: Metadata = { title: "Projects v8 — Stories + Numbers" };

export default function ProjectsV8Page() {
  return <MobileProjectsStoriesNum />;
}
